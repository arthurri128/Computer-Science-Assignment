class Warehouse:

    # Privatising the attributes and setting them to self.__
    def __init__(self, rows, cols, grid):
        self.rows = rows
        self.cols = cols
        self.grid[rows][cols]
    
    # Adds location within the warehouse
    def add_location(location):
        self.grid[location.rows][location.cols] = location